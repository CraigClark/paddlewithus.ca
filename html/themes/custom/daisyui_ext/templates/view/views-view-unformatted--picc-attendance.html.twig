{#
/**
 * @file
 * Theme override for unformatted rows in the coach attendance view.
 *
 * Matches the registration-view spacing between stacked cards.
 * Grouping headings are handled by the grouping template.
 */
#}
{% if title %}
  <h3 class="text-lg font-semibold mt-4 mb-2 border-b border-base-300 pb-2">{{ title }}</h3>
{% endif %}
<div class="flex flex-col gap-2 mb-4">
  {% for row in rows %}
    {{- row.content -}}
  {% endfor %}
</div>
